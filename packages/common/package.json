{
  "name": "@scheduler-platform/common",
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  }
}
